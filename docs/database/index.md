### Supabase

Use the Supabase JS client (`@supabase/supabase-js`). Server-side queries use the service role key (server only); client-side uses the anon key. Row-level security (RLS) should be enabled on all tables.

### Primeiro ponto
1) Tabela de founders - @docs/features/form/sections/founders.md

